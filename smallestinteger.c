/*Find the smallest integer in a list of numbers using a function called min().
Function needs to take two integers as input and returns the smallest of the two.
This function will use an if statement with a condition that contains either "less than" or "greater than."

Then use the function to go through an entire list */


#include <stdio.h>
